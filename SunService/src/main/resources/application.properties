spring.application.name=SunService
# PostgreSQL Verbindung
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:5432/sundb
spring.datasource.username=postgres
spring.datasource.password=Mnif4674!?

# JPA / Hibernate Einstellungen
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Zeigt die SQL-Befehle in der Konsole an (hilfreich beim Debuggen)
spring.jpa.show-sql=true

# Lokal erlauben wir 4200, auf dem Server die IP
app.cors.allowed-origins=http://localhost:4200,http://87.106.173.25